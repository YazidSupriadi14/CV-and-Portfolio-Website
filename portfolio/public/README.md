# Public Assets — TODO

- `resume.pdf` — ✅ added.
- `favicon.ico` — site favicon (32x32 or 64x64).
- `og-image.png` — 1200x630 social share preview image, referenced by
  `openGraph.images` in `src/app/layout.tsx` (not yet configured — add once
  the image exists).
